# 剩余事项执行计划（2026年9月22日）

> 来源：用户指令“按照建议依次执行，先落库再执行”。
> 方法：每项先冻结入口条件＋执行步骤＋验收门（落库），再执行当前可执行部分；
> 需外部输入的项冻结等待，不虚构执行。
> 跟踪：`TASK_PLAN.md` 步骤 17；日志：`EXEC_LOG.md`（append-only）。

## 状态总览

| 子项 | 内容 | 状态 | 验收 |
|------|------|------|------|
| REM-1 | 真 Key 灰度 runbook | 🟡 已冻结，等输入 | Key 到位后可直接执行 |
| REM-2 | Linux 50k 验收 | 🟡 已冻结，等节点 | 节点到位后可直接执行 |
| REM-3 | JA4 前置复核 | ✅ 已完成（仍 OUT，前置零变化） | 本文件即证据 |
| REM-4 | 完工冻结盘点 | ✅ 已完成（本轮） | 状态报告＋冻结 |

---

### REM-1 真 Key 灰度 runbook（BLOCKED：待用户提供三家 Key）

**入口条件**：用户提供 `oxylabs / brightdata / netnut`（或等效三家）API Key＋入口 host:port，
经 secret 管理注入（禁进仓库，OPERATION §3 第 1 步）。

**执行步骤**（Key 到位后按序，每步独立可回滚）：
1. staging 配 1 家 1 国：`main.rs` 初始池对应 `mock-x` 换真实 `ip:port＋账密`，
   provider 改真名，country 照实（OPERATION §3 第 2 步原文）。
2. 观察 1 个 60s 套利周期：`query_provider_sla` 5min 窗＋Grafana 403 比面板；
   阈值 <80→0 / >95→100 不动。
3. 计费对账：`tenant.total_bytes × tier单价` 与供应商账单按周对（另起 GW-R2）。
4. 逐家逐国放开，每步至少观察 1 个 60s 周期；异常即 `adjust_vendor_weight(vendor,country,0)`
   手动摘除（无需重启，R2-1 语义）。

**验收门**：三家×三国 200 率与 mock 基线持平（±2%）＋套利日志有 derate/restore 行＋
CH 落库 provider 真名行。**本轮状态：计划已冻结，等待 Key，无代码变更。**

---

### REM-2 Linux 50k 验收（BLOCKED：待性能节点）

**入口条件**：x86_64 Linux 节点（内核 ≥5.8，独占网卡，`deploy/sysctl.conf` 已应用＋`sysctl -p` 确认）。

**执行步骤**（节点到位后按序）：
1. 同步仓库＋`cargo build --release`（`lto=fat` 全量约数分钟，属正常）。
2. 起依赖＋mocks＋网关（默认 env）：`docker compose up -d`＋detached 三 mocks＋网关。
3. 基准：`cargo test --release bandit`（8 臂 <200ns 复核）＋`cargo bench --workspace`
   （release 全量 bench，首次跑通即基线）。
4. 压测：`wrk -t16 -c50k -d60s http://node:8080/`（或等效 `log/load_probe.py` 放大版）；
   目标：P99 <1ms（选路）／端到端 P99 <100ms／QPS 上限探测＋`dmesg` 无 OOM。
5. 回归：curl 六用例＋XLEN/CH 涨（Linux 首轮全链路）。

**验收门**：选路 <200ns（release）＋端到端 P99 达标＋四门绿（Linux 侧）。
**本轮状态**：计划已冻结，等待节点，无代码变更。Windows dev 基线仅供参考
（P99 65.6ms/QPS 986，GW-5；非生产承诺）。

---

### REM-3 JA4 前置复核（✅ 本轮已执行：仍 OUT）

- 复核命令与结果（2026-09-22）：
  - `gateway/Cargo.toml`：`pingora{,-core,-proxy,-load-balancing} = "0.6"`，无 boringssl/utls
    依赖（注释行确认 0.4 不可用原因仍在）。
  - `gateway/src/gateway.rs:163-164`：`is_tls = false`（注释 `frozen: no uTLS/Boring`）。
  - mocks：`log/mock_upstream.py` 纯 `http.server`（plain-HTTP，无 TLS 对端）。
- 结论：自 SPIKE-R2 起零变化，NO-GO 三条件（无 TLS 面／无注入点／fork 冲突）全在，
  JA4 延续 OUT；P3-1 风险溢价仍为诚实替代。**本轮状态：已执行完毕，无代码变更。**

---

### REM-4 完工冻结盘点（✅ 本轮）

| 维度 | 状态 |
|---|---|
| 步骤 | 1~16 全✅（17 本轮冻结） |
| 提交 | 11 个（`3130042`～`7c195ce`），工作树干净 |
| 单测 | 144 过／4 ignored＋4 真 live；release bandit <200ns 持绿 |
| 数据面 | curl 六用例＋缺 Host 400＋/metrics 200；XLEN/CH 链路活；CH 行 `free-gh0\|公网exit\|200` |
| 在线进程 | 网关 4544＋relay/list/mocks（E2E 环境）；四容器 Up（CH unhealthy 系已知探针 artifact） |
| 文档 | OPERATION（16 轮同步）＋7 份 dated 计划＋Grafana 5 面板＋EGRESS_MESH＋SPIKE_R2 |
| 冻结项 | 真 Key（等）／Linux（等）／JA4（OUT）／mismatch 执法（Phase 4 开关默认关）／库热加载（OUT） |

冻结语义：非阻塞项全部收官；阻塞项入口条件＋步骤＋验收门已冻结，到位即执行，
无需重新勘察。
