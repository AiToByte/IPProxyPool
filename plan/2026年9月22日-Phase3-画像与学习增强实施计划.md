# Phase 3 画像与学习增强 Implementation Plan（2026年9月22日）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **Goal:** 免费线画像（exit-IP 国家一致性观察）＋学习加速（免费臂短窗遗忘＋风险溢价）＋
> 免费独立套利（池级成功率分档调权）＋健康复合分（成功率×延迟×留存）＋报表可分（Grafana free 面板），
> 全程默认行为冻结（存量断言零修改），新增皆可回滚。
>
> **Architecture:** bandit 臂按 tier 分档遗忘（free 短窗）＋free 臂 UCB 风险溢价（JA4 高风控面的诚实替代）
> → VendorArbitrageWorker free 分支（快照枚举 free-* provider×country → 池级 SLA → 分档 scale）
> → free `Health::composite`（score×留存因子，`score()` 本体冻结）→ `geo.rs`（maxminddb 可选集成，
> 无库降级观察＋mismatch 计数）→ Grafana 第 5 面板＋OPERATION＋env。
>
> **Tech Stack:** Rust（maxminddb 0.32 新增——本轮唯一新 crate，经勘察：cargo 可达，
> crate 包内无测试 mmdb，Github raw 在沙箱被墙，GeoLite2 需 license key；见 §1-F0）。
> 其余零新依赖。
>
> **Deviations locked（勘察结论，以本节为准）：**
> - F0 · GeoIP 数据文件沙箱不可得（license 墙＋github 墙＋crate 包无测试数据，三重确认，
>   见 EXEC_LOG 步骤 12 勘察条）。`geo.rs` 实现完整集成（open/readfile/iso_code 比对），
>   但沙箱内只能验证 Disabled 降级＋纯逻辑＋指标；全路径编译通过＋走查，生产需运营方配库。
>   这是诚实降级交付，不是跳过——计划内显式标注。
> - JA4 漂移门：OUT（SPIKE-R2 NO-GO 依然成立：无 TLS 面＋上游 plain-HTTP 冻结＋fork 冲突，
>   本轮无任何变化）。替代：free 臂 UCB 风险溢价（P3-1，同 F5 动机“免费≈全数据中心 IP→高风控面”）。
> - dLinUCB-change-detection／P2C 选路：OUT（per-tier forgetting 已覆盖非平稳吸收；
>   换选路策略属架构改动，R2-6/v2 已两次冻结）。
> - exit-country mismatch 策略：只观察不执法（metric＋debug；DB 陈旧时执法会误杀好节点，
>   注释写明，执法开关留 Phase 4）。
>
> **File map:**
> - Modify: `gateway/Cargo.toml`＋`Cargo.lock`（maxminddb）、`gateway/src/bandit.rs`
>   （分档遗忘＋风险溢价＋2 单测）、`gateway/src/router.rs`（`scale_vendor_weights`＋1 单测）、
>   `gateway/src/vendor_arbitrage.rs`（free 分支＋2 单测）、`gateway/src/free_pool.rs`
>   （composite＋geo 观察接线＋3 单测）、`gateway/src/metrics.rs`（geo 计数＋1 单测）、
>   `gateway/src/main.rs`（`mod geo`＋GeoDb 装配＋env）、`deploy/grafana/dashboard.json`
>   （第 5 面板）、`docs/OPERATION.md`、`docker-compose.yml`。
> - Create: `gateway/src/geo.rs`（集成＋单测）。
>
> ## §1. 前沿依据（沿 v2 §1-F5/F6/F8，不重复检索）
>
> - **F5 回顾**：免费≈全数据中心 IP→JA4 高风控面→本计划 P3-1 风险溢价（可落地的诚实子集）。
> - **F6 回顾**：非平稳三法（discount/window/change-point）；R2-6 coarse-restart 够付费线，
>   本计划 per-tier 短窗够免费线（churn 高一个量级，IPinfo F7）。
> - **F8 回顾**：composite＝成功率＋延迟＋会话保持＋硬目标可达；本计划取前三可落地项
>   （硬目标可达＝FullCheck 存活本身，已在分母；不搞黑盒加权）。
>
> ## §2. Env 总表（新增 1 个）
>
> | Key | 默认 | 语义 |
> |-----|------|------|
> | `GEOIP_MMDB_PATH` | `""` | GeoLite2-City.mmdb 路径；空／不可读→Disabled 降级（指标可见） |
>
> ---

### P3-1: bandit 分档遗忘＋免费风险溢价

**Files:** `gateway/src/bandit.rs`（`forget_every_for_tier`＋`update` 分档＋`FREE_RISK_PREMIUM`＋2 单测）

- [ ] **Step 1: 写失败单测**

```rust
#[test]
fn forgetting_is_faster_for_free_tier() {
    // 免费臂 1k 次即 blend（short window），付费臂 10k；blend 数学形态与 R2-6 同（90/10）。
    // 做法：free 臂 update 1000 次→updates%FORGET_EVERY_FREE==0 处已触发（经 A_inv 非单位阵断言）；
    // residential 臂 update 1000 次→A_inv 仍恒等（用 trace/行列式断言，或对比两者 A_inv 不等）。
}

#[test]
fn free_arm_pays_risk_premium() {
    // 同 context 下 free 臂 UCB < residential 臂（差值 == premium）；DC/mobile 行为不变。
}
```

- [ ] **Step 2: 运行确认失败** Run: `cargo test --workspace forgetting_is_faster free_arm_pays` Expected: FAIL
- [ ] **Step 3: 最小实现**

```rust
/// R2-6 节拍（付费线）；免费线短窗（churn 高一个量级，v2-F7：平均可见 4.56 天）。
pub const FORGET_EVERY_N_UPDATES: u64 = 10_000; // 存量冻结
pub const FORGET_EVERY_FREE_UPDATES: u64 = 1_000;
/// P3 免费风险溢价（v2-F5：免费≈全 DC IP→高 JA4 风控面；全 JA4 门需 TLS 面仍 out，
/// 此处为诚实替代：可解释常数惩罚，不碰 context 维度）。
pub const FREE_RISK_PREMIUM: f64 = 0.15;

pub fn forget_every_for_tier(tier: &str) -> u64 {
    if tier.eq_ignore_ascii_case("free") { FORGET_EVERY_FREE_UPDATES } else { FORGET_EVERY_N_UPDATES }
}
// update() 内：if state.updates.is_multiple_of(forget_every_for_tier(&self.tier)) { apply_forgetting }
// （BanditArm.tier 字段转正：去 #[allow(dead_code)]，注释写明 P3 消费）。
// compute_ucb_score 尾部：- 0.05 * cost_weight 后追加 free 惩罚：
// if self.tier.eq_ignore_ascii_case("free") { score -= FREE_RISK_PREMIUM }
```

约束：`update` 内读 `self.tier`（`&self` 已有）；存量 `forgetting_fires_every_10k` 单测语义冻结
（residential 臂行为不变）；release 8 臂 bench 仍 <200ns（多一次字符串比较，实测确认）。

- [ ] **Step 4: 运行确认通过** Run: `cargo test --workspace bandit` Expected: 全 PASS（含存量 7＋遗忘 2）
- [ ] **Step 5: Verify（fmt＋clippy）**

---

### P3-2: 免费独立套利（分档调权＋worker free 分支）

**Files:** `gateway/src/vendor_arbitrage.rs`（`free_pool_action`＋`audit_free_once`＋2 单测）、
`gateway/src/router.rs`（`scale_vendor_weights(prefix→(vendor,country), factor)`＋1 单测）

- [ ] **Step 1: 写失败单测**

```rust
#[test]
fn free_pool_action_thresholds() {
    // 池级成功率：<50 → Some(0.0)（摘除，TTL/复检自愈）；50~80 → Some(0.5)（半权）；
    // ≥80 → None（hold；free 永不自动抬到付费量级，恢复走复检路径）。
    assert_eq!(free_pool_action(49.9), Some(0.0));
    assert_eq!(free_pool_action(50.0), Some(0.5));
    assert_eq!(free_pool_action(79.9), Some(0.5));
    assert_eq!(free_pool_action(80.0), None);
    assert_eq!(free_pool_action(100.0), None);
}

#[test]
fn scale_vendor_weights_proportional() {
    // free-a/US 两节点 weight 10/6 → factor 0.5 → 5/3（round；下限 0）；
    // 非 free 前缀节点不动；factor 0 → 全 0（matches 过滤即摘除，可恢复）。
}
```

- [ ] **Step 2: 运行确认失败** Run: `cargo test --workspace free_pool_action scale_vendor` Expected: FAIL
- [ ] **Step 3: 最小实现**

```rust
// vendor_arbitrage.rs
/// P3 免费独立套利：池级成功率→缩放因子（None＝hold；free 恢复走复检/health 路径，不抬权）。
pub fn free_pool_action(success_rate: f64) -> Option<f64> {
    if success_rate < 50.0 { Some(0.0) } else if success_rate < 80.0 { Some(0.5) } else { None }
}
// router.rs（adjust_vendor_weight 旁，写时复制同模式）：
pub fn scale_vendor_weights(&self, vendor: &str, country: &str, factor: f64) {
    // weight = (w * factor).round()，factor≤0 即摘除（可恢复：复检 upsert 重置 health 权重）。
}
// worker：audit_once 追加 free 分支（audit_free_once）：快照枚举 provider starts_with("free-") 的
// 去重 (provider, country) 对 → analytics.query_provider_sla → free_pool_action → scale＋warn 日志；
// CH 错 hold（沿现有 degraded 语义）。
```

约束：付费 `arbitrage_action` 80/95 冻结不动；free 分支与付费循环解耦（独立函数，单测 pure 部分；
worker 接线需 CH，live-gated，注释写明）。

- [ ] **Step 4: 运行确认通过** Run: `cargo test --workspace arbitrage router` Expected: 全 PASS
- [ ] **Step 5: Verify（fmt＋clippy）**

---

### P3-3: Health 复合分（留存因子）

**Files:** `gateway/src/free_pool.rs`（`Health::composite`＋`weight` 切 composite＋1 新单测；
存量 `health_score_math` 零修改——冻结验证）

- [ ] **Step 1: 写失败单测**

```rust
#[test]
fn composite_rewards_streak() {
    // composite＝score×(0.5＋0.5×min(1,streak/3))：fresh(0.5, streak0)→0.25；
    // 50 连成功→factor 1→composite==score；连败→更低（ behaviors，不卡精确值只卡方向＋边界）。
    // weight() 改走 composite 后：50 连成功仍 20；连败仍 1（存量断言复算通过即冻结成立）。
}
```

- [ ] **Step 2: 运行确认失败** Run: `cargo test --workspace composite_rewards` Expected: FAIL
- [ ] **Step 3: 最小实现**

```rust
impl Health {
    /// P3 复合分（ProxyStats 方法学可落地子集）：成功率×延迟惩罚（score 本体冻结）
    /// ×留存因子（streak≥3 满 1；ProxyStats 会话保持 72~97% 的连续映射，不搞硬阈值抖动）。
    pub fn composite(&self) -> f64 {
        let retention = 0.5 + 0.5 * ((self.streak as f64) / (TRUSTED_MIN_STREAK as f64)).min(1.0);
        self.score() * retention
    }
    // weight() 内 self.score() → self.composite()（cap/保底逻辑不动）。
}
```

约束：`score()`/`trusted()` 一字不动；存量 `health_score_math` 必须零修改通过
（fresh score 0.5／50 连 20／慢<快／连败 1——逐项在实现后复算，见 Step 4）。

- [ ] **Step 4: 运行确认通过** Run: `cargo test --workspace health_ composite_ backoff_` Expected: 全 PASS
  （若存量任一断言红→停，说明 composite 映射越界，回滚 weight 切线并记录）
- [ ] **Step 5: Verify（fmt＋clippy）**

---

### P3-4: GeoIP 集成（maxminddb＋降级观察）

**Files:** `gateway/Cargo.toml`（`maxminddb = "0.32"`）＋ Create `gateway/src/geo.rs`（`GeoDb`＋
`exit_matches_source`＋`note` 指标接口＋3 单测）；Modify `gateway/src/metrics.rs`
（`geoip_lookups{result}`＋`geoip_mismatch_total`＋1 单测）、`gateway/src/free_pool.rs`
（Worker 观察接线）、`gateway/src/main.rs`（`mod geo`＋装配＋env）

- [ ] **Step 1: 写失败单测**

```rust
#[test] fn disabled_db_skips_lookup() // GeoDb::disabled().country("1.2.3.4")==None＋reason=="disabled"
#[test] fn exit_matches_source_matrix() // fail-open：缺失即跳过 true；仅两码皆知且不等才 false
#[test] fn metrics_geo_rendered() // note_geo_lookup("disabled")＋note_geo_mismatch()→两行存在
```

- [ ] **Step 2: 运行确认失败** Run: `cargo test --workspace disabled_db_skips exit_matches_source metrics_geo` Expected: FAIL
  （Cargo 加依赖后首编约 1min，属正常；`cargo fetch` 先行亦可）
- [ ] **Step 3: 最小实现**

```rust
/// P3 exit-IP 画像（maxminddb 可选集成）。
/// 缺库／不可读／IP 非法一律降级为观察跳过（Disabled/lookup-fail 计数，不执法——见计划）。
pub enum GeoDb {
    Disabled { reason: &'static str },
    Live(maxminddb::Reader<Vec<u8>>),
}
impl GeoDb {
    pub fn open(path: &str) -> Self; // 空串→Disabled("unset")；open 失败→Disabled("unreadable")＋warn
    pub fn disabled() -> Self;
    /// 有库且命中返回 ISO 国家码（大写归一）；否则 None（调用方记 lookup 结果）。
    pub fn country(&self, ip: &str) -> Option<String>;
}
/// 纯策略：declared 缺省 ZZ 免检（true）；否则大小写无关相等。
pub fn exit_matches_source(declared: Option<&str>, looked_up: Option<&str>) -> bool;
```

Worker 接线（`run_once` FullCheck 成功分支，基线降级分支不查——无 exit_ip）：
```rust
// geo 观察（只观察不执法）：有 exit_ip 且 geo enabled → country() →
//   Some(c)：note_geo_lookup("hit")；mismatch 则 note_geo_mismatch()＋debug（执法留 Phase 4）
//   None：note_geo_lookup("miss")
// Disabled：首 tick 一次 note_geo_lookup("disabled")（每轮都记会刷计数器——用 tick==1 条件，注释写明）
```
`FreePoolWorker` 加 `geo: Option<Arc<GeoDb>>` 字段（None＝Disabled 快捷；main 有 path 才建 Live）；
metrics 加 `note_geo_lookup(result)`（result∈hit/miss/disabled/error，调用方保证）＋
`note_geo_mismatch()`（counter）＋render 两组。

约束：沙箱无库——全路径（Live lookup）编译通过＋走查，单测只锁 Disabled/纯逻辑/指标；
OPERATION 写明生产配库步骤（GeoLite2  license＋路径＋重启）；`maxminddb::Reader` 须 Send+Sync
（Arc 共享；若非，包 parking_lot::Mutex 并注释——实现时以编译器为准）。

- [ ] **Step 4: 运行确认通过** Run: `cargo test --workspace geo` Expected: PASS（4 单测）
- [ ] **Step 5: Verify（fmt＋clippy）**

---

### P3-5: 报表＋运维落盘

**Files:** `deploy/grafana/dashboard.json`（第 5 面板 free-tier SLA/水位）＋
`docs/OPERATION.md`（§4 两行＋§6 一行＋GeoLite2 配库步骤）＋`docker-compose.yml`
（`GEOIP_MMDB_PATH` 注释示例）

- [ ] **Step 1: 最小实现**（无 Rust 单测；JSON 有效性由 P3-6 回归命令断言）
  - 面板 5（沿既有 4 面板 PromQL 风格）：`free_pool_nodes_by_proto` 水位＋
    `free_pool_verify_total` fail 分布＋`geoip_mismatch_total`（三 target，一 panel，注释写查询）。
  - OPERATION §4：`| 免费套利 | free_pool_action 50/80 分档 | <50 摘除（复检自愈）/50~80 半权/≥80 hold；付费 80/95 不动 |`＋
    `| GeoIP 画像 | geoip_lookups/geoip_mismatch_total | 无库 Disabled 只观察不执法；mismatch 突增＝源站地理造假或库陈旧，先查库版本再定 |`
    §6：`- GeoLite2 配库：MaxMind 账号取 license→下 GeoLite2-City.mmdb→挂载→GEOIP_MMDB_PATH→重启（热加载不做）；无库默认 Disabled，免费线行为不变。`
  - compose：`GEOIP_MMDB_PATH: "/data/GeoLite2-City.mmdb"`（注释态）。
- [ ] **Step 2: 运行确认通过** Run: `python -c json.load` 有效＋panel 数 5（P3-6 一并执行，此处先自查）

---

### P3-6: 最终门禁＋回归＋落库

- [ ] **Step 1: 四门全跑** `cargo fmt --check`；`clippy --all-targets -D warnings`；
  `cargo test --workspace`（127＋P3 新增约 12＝139±2，以实测为准）；`-- --ignored`（先验 PONG/Ok，无 SKIP）；
  `cargo bench --no-run`；`cargo test --release bandit`（8 臂仍 <200ns：P3-1 多一次 tier 比较，实测确认）
- [ ] **Step 2: 存量＋E2E 回归** gw E2E 环境复用（relay/list 脚本已提交）：默认六用例＋socks E2E 五断言重跑一遍
  （free  Tick 60s 节拍内 pool 回 1；`composite` 上线后权重仍 1..20，`by_proto` 行在）；
  XLEN/CH 涨；dashboard JSON 有效＋5 panels。
- [ ] **Step 3: 落库** plan 本表全✅＋EXEC_LOG append-only（含 maxminddb 诚实记录＋bench 数＋PID/日志）＋
  TASK_PLAN 步骤 12 ✅；禁未授权 commit——用户确认后统一提交。

---

## §3. 状态总览

| 子项 | 内容 | 优先级 | 状态 | 验收 |
|------|------|--------|------|------|
| P3-1 | bandit 分档遗忘＋风险溢价 | P0 | ✅ 已完成（2026-09-22：free 1k 短窗＋UCB 风险溢价 0.15；2 单测；129 过。诚实记录：gap 实测 0.10（premium−cost 差），断言按构成式锁定） | 短窗数学＋溢价方向 |
| P3-2 | 免费套利＋scale＋worker 分支 | P0 | ✅ 已完成（2026-09-22：50/80 分档＋等比缩放＋audit_free_once；2 单测；131 过） | 50/80 分档＋比例缩放 |
| P3-3 | Health 复合分 | P0 | ✅ 已完成（2026-09-22：composite＝score×留存因子＋weight 切换；存量 `health_score_math` 零修改通过；132 过） | 存量零修改通过 |
| P3-4 | GeoIP 集成＋降级 | P0 | ✅ 已完成（2026-09-22：GeoDb open/disabled/country＋fail-open 矩阵＋观察接线＋指标；5 单测；136 过。诚实记录：沙箱无库，全路径编译＋走查；exit 语义修正为 fail-open（计划原文“任一缺失 false”已更正，以实现为准） | Disabled＋纯逻辑＋指标 |
| P3-5 | 报表＋运维 | P1 | ✅ 已完成（2026-09-22：第 5 面板＋OPERATION§4 两行/§6 配库＋compose env） | 5 面板＋OPERATION |
| P3-6 | 门禁＋回归 | 门禁 | ✅ 已完成（2026-09-22：136 过/4 真 live/release bandit<200ns＋存量六用例＋E2E 重跑＋dashboard 有效，见 EXEC_LOG 步骤 12 条） | 139±2＋E2E 重跑 |

## §4. 不做（explicitly out → Phase 4/待输入）

- **JA4 漂移门**（SPIKE-R2 NO-GO 延续；P3-1 风险溢价为诚实替代）。
- **dLinUCB change-detection／P2C 选路**（per-tier forgetting 已覆盖；换策略另议）。
- **mismatch 执法**（观察→执法需运营数据，Phase 4 开关）。
- **GeoLite2 自动更新**（cron＋license 运维事项，不进网关）。
- **真 Key 灰度／Linux 50k 验收**（仍待用户输入，与 Phase 3 并行不阻塞）。

## Self-Review

1. **Spec coverage:** 画像→P3-4（降级观察）＋P3-5 面板；免费臂上浮→P3-1 溢价；
   per-tier forgetting→P3-1；独立套利→P3-2；composite→P3-3；CH 明细→P3-5 面板
   （provider free-* 原生可分，报表侧落地，OPERATION 注查询）。
2. **Placeholder scan:** 无 TBD/TODO；GeoIP 全路径缺测已在 F0/约束/P3-6 如实声明三处。
3. **Type consistency:** `forget_every_for_tier(&str)->u64`；`free_pool_action(f64)->Option<f64>`；
   `scale_vendor_weights(&str,&str,f64)`；`Health::composite()->f64`（score 冻结）；
   `GeoDb::{open,disabled,country}`＋`exit_matches_source(Option,Option)->bool`。
4. **存量风险：** `update()` 加 tier 分支——residential 路径与 R2-6 逐行等价（单测守护）；
   `weight()` 切 composite——存量四断言复算守护（P3-3 Step 4 熔断条件）；
   `audit_once` 追加 free 分支——付费循环零改动；`ProxyNode` 字段不动（tier 已有）。
5. **门禁诚实性：** P3-4 全路径缺测显式声明；单测数实测为准；release bandit 复测（P3-1 加比较）。
