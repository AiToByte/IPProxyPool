# DOC-R1 文档一致性修复 Implementation Plan（2026年9月22日）

> 来源：下一阶段审计（dashboard PromQL vs exposition＋OPERATION 断言 vs 代码）。
> 结论：dashboard 5 面板 PromQL 与 `render()` 输出逐名核对，零漂移，无需改动；
> OPERATION 发现 1 中 2 小三处漂移，本计划只修文档，零代码变更。
> 跟踪：`TASK_PLAN.md` 步骤 16；日志：`EXEC_LOG.md`（append-only）。

## 状态总览

| 子项 | 内容 | 优先级 | 状态 | 验收 |
|------|------|--------|------|------|
| D1 | §3 套利语义更新（R2-1 权重语义） | P1 | ✅ 已完成（2026-09-22：真权重即时语义） | 文本与 `adjust_vendor_weight` 一致 |
| D2 | §4 后台并发行（补 free_pool＋握手口径） | P2 | ✅ 已完成（2026-09-22：supervise 名单＋建链/握手） | 文本与 main/prober/pool 一致 |
| D3 | §4 租户行补 free $0 计价 | P2 | ✅ 已完成（2026-09-22） | 文本与 `price_per_gb` 一致 |
| D4 | 落库＋提交确认 | 门禁 | ✅ 已完成（2026-09-22：见 EXEC_LOG 步骤 16 条） | 重读核对＋提交 |

## 拆解

### D1 §3 第 4 行套利语义（中）
- 现状文本：“降权只是 `retain` 摘除，恢复需重启（GW-1 `adjust_vendor_weight`
  retain/remove 垫片，GW-R2 改真权重）”——R2-1 已改为只改数字不增删
  （`weight==0` 选路过滤、池内保留，`restore→100` 即时恢复，无需重启），
  EXEC_LOG R2-1 条为证。GW-4 起即真权重，无“垫片”概念残留。
- 改法：改为“降权即 `weight→0`（选路过滤摘除，池内保留可恢复），恢复 `weight→100`
  即时生效，无需重启（R2-1 起真权重语义；`matches` 过滤 `weight==0`）”。

### D2 §4 后台并发行（小）
- 现状文本只列 CB/sink/arbitrage 由 supervisor 托管＋prewarmer“真 TCP 探测”——
  main 已用 `supervise` 包 free_pool（Task 12），prewarmer P2-6 起对 socks 做 greeting。
- 改法：名单加 free_pool；“真 TCP 探测”改“建链/握手探测（http 建链＋socks greeting）”。

### D3 §4 租户行（小）
- 现状只列 DC/Res/Mobile 单价——`price_per_gb("free")==0`（Task 1）在文本无处交代。
- 改法：行尾加“；Free $0（计量字节但不计费，FreePool 节点 tier）”。

## 不做
- dashboard（已逐名验证零漂移）；EGRESS_MESH/SPIKE（冻结结论）；§2 curl（Host 默认带，
  仍 200，无漂移）；P99 基线注（65ms 在 49~65 实测带内，不动）。
