# Phase 4 执法与运维 Implementation Plan（2026年9月22日）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **Goal:** Phase 3 预留的两项可落地收尾——exit-国家分歧执法开关（默认关闭，只观察→可执法）
> ＋GeoLite2 库更新脚本与文档（license 墙内可执行的运维件）。全程默认行为冻结，新增可回滚。
>
> **Architecture:** `geo_verdict` 纯判定（match/mismatch/skip）→ Worker 按
> `GEOIP_ENFORCE_MISMATCH` 开关把 mismatch 映射为复检失败（backoff＋`geo_fail` 指标，
> 复用现有失败语义）→ `deploy/geoip_update.py`（license key 换库＋原子替换）＋
> OPERATION 定时任务文档。
>
> **Tech Stack:** Rust（零新依赖）＋ Python 标准库脚本（沿 `log/` 脚本惯例，落 `deploy/`）。
>
> **Deviations locked（以本节为准）：**
> - JA4：仍 OUT（前置无任何变化：上游仍 plain-HTTP mocks，Pingora 仍 0.6 无 TLS 可调面，
>   fork 冲突仍在；立项时复核确认，见 EXEC_LOG 步骤 14 勘察条）。
> - 沙箱无库：执法全路径沙箱不可触发（Disabled 下永不 mismatch）；单测锁纯判定＋接线
>   编译＋E2E 验证“默认关闭零行为变化”（诚实声明，沿 P3-4 F0 口径）。
> - 热加载不做（重启生效；库文件 memmap 常驻，换库必须重建 Reader，注释写明）。
>
> **File map:**
> - Modify: `gateway/src/geo.rs`（`GeoVerdict`＋`geo_verdict`＋1 单测）、
>   `gateway/src/free_pool.rs`（`observe_geo` 返回判定＋ enforcing 映射＋`FreePoolConfig.geo_enforce`＋1 单测）、
>   `gateway/src/metrics.rs`（`geo_fail` 结果值注释＋render 无需改动——`note_free_verify` 通用）、
>   `gateway/src/main.rs`（env 接线）、`docs/OPERATION.md`（§4 一行＋§6 定时任务）、
>   `docker-compose.yml`（env＋volume 注释）。
> - Create: `deploy/geoip_update.py`（更新脚本）.
>
> ## §2. Env 总表（新增 1 个）
>
> | Key | 默认 | 语义 |
> |-----|------|------|
> | `GEOIP_ENFORCE_MISMATCH` | `"0"` | 为 1 且库 Live 时，exit-国家实锤分歧按复检失败计（backoff＋`geo_fail`）；默认只观察 |
>
> ---

### P4-1: mismatch 执法开关

**Files:** `gateway/src/geo.rs`（`GeoVerdict`＋`geo_verdict`＋1 单测）、
`gateway/src/free_pool.rs`（`observe_geo→check_geo`＋enforce 映射＋config 字段＋1 单测）、
`gateway/src/main.rs`（env 接线）

- [ ] **Step 1: 写失败单测**

```rust
#[test]
fn geo_verdict_matrix() {
    // 纯判定：match→Match；实锤分歧→Mismatch；声明缺省/库miss/disabled→Skipped（fail-open延续）。
    // designated vs looked_up Nei-noise：用 disabled-geo（None）与 ZZ 组合覆盖 Skipped。
}

#[test]
fn enforce_maps_mismatch_to_failure() {
    // Worker 级：enforce 开＋Mismatch→`note_verify_failed`（backoff 快照不可见但条目保留）；
    // enforce 关＋Mismatch→条目仍在池（只记指标）。用 Registry＋Health 直接驱动，不起 Worker。
}
```

注：第二个单测需 Worker 映射逻辑可直调——抽 `apply_geo_verdict(registry, metrics, addr, verdict, enforce)`
自由函数（worker 与单测共用），签名以实现为准，计划只锁语义。

- [ ] **Step 2: 运行确认失败** Run: `cargo test --workspace geo_verdict enforce_maps` Expected: FAIL
- [ ] **Step 3: 最小实现**

```rust
// geo.rs
pub enum GeoVerdict { Match, Mismatch, Skipped }
pub fn geo_verdict(declared: Option<&str>, looked_up: Option<String-ish>) -> GeoVerdict;
// fail-open：任一缺失→Skipped；ZZ→Match；两码皆知不等→Mismatch（沿 exit_matches_source）。
// exit_matches_source 本体冻结（单测守护），verdict 为其上层映射。

// free_pool.rs
// observe_geo 改返回 GeoVerdict（指标注记保留；调用点：FullCheck 成功分支）。
// 成功分支内：if verdict == Mismatch && self.config.geo_enforce { note_verify_failed(addr, now);
//   metrics.note_free_verify("geo_fail"); }（注释：backoff 语义复用，TTL 内保留＋自动恢复）。
// FreePoolConfig 加 `pub geo_enforce: bool`（main 从 env 组装；字面量构造点同步——编译器指引）。
// metrics 注释 `result` 白名单追加 geo_fail（render 通用实现不动）。
```

约束：默认关闭（`geo_enforce=false` 时 mismatch 路径与 Phase 3 逐行等价）；
`exit_matches_source` 一字不动；enforce 只影响 mismatch（miss/disabled 永不执法）。

- [ ] **Step 4: 运行确认通过** Run: `cargo test --workspace geo free_pool` Expected: 全 PASS
- [ ] **Step 5: Verify（fmt＋clippy）**

---

### P4-2: GeoLite2 更新脚本＋文档

**Files:** Create `deploy/geoip_update.py`；Modify `docs/OPERATION.md`（§6 定时任务块）、
`docker-compose.yml`（volume＋env 注释补全）

- [ ] **Step 1: 最小实现**（无 Rust 单测；脚本自验证见 Step 2）

```python
# deploy/geoip_update.py：MAXMIND_LICENSE_KEY（env 必填）＋--edition（默认 GeoLite2-City）
# ＋--out-dir（默认 ./data）。流程：拼下载 URL→urllib 取 tar.gz→内存解 tar→提 .mmdb→
# 写临时文件→os.replace 原子替换→打印版本行。无 key 时 exit 2＋usage（自验证断言点）。
# 零第三方依赖（urllib＋tarfile＋os＋sys＋argparse，标准库）。
```

OPERATION §6 追加定时任务块：
`- GeoLite2 自动更新（P4）：每周三 03:00（UTC）跑 \`deploy/geoip_update.py\`（\`MAXMIND_LICENSE_KEY\`
环境传入，不要落盘）；Linux cron 例＋Windows schtasks 例（见下）；更新后重启网关（热加载不做）；
网关启动行 \`[GeoIP] live DB loaded\` 即生效，\`geoip_lookups{result="hit"}\` 开始涨。`
compose：`GEOIP_MMDB_PATH` 行旁加 volume 注释（`./data:/data:ro` 网关挂载示例，注释态）。

- [ ] **Step 2: 运行确认通过** Run: 无 key 跑脚本→exit 2＋usage 行；`python -m py_compile` 过；
  `--help` 正常。Expected: 干跑证据（不碰外网，不耗 license 配额）

---

### P4-3: 最终门禁＋回归＋落库

- [ ] **Step 1: 四门全跑** `cargo fmt --check`；`clippy --all-targets -D warnings`；
  `cargo test --workspace`（141＋P4 新增约 3＝144±2，以实测为准）；`-- --ignored`
  （先验 PONG/Ok，无 SKIP）；`cargo bench --no-run`（bandit 未动，release bandit 跳过并注明理由）
- [ ] **Step 2: 存量＋E2E 回归** 默认网关六用例＋缺 Host＋/metrics；FREE E2E 重跑
  （relay/list 脚本已提交；enforce 默认关→pool/水位/出包与 Phase 3 一致；
  额外断言：`GEOIP_ENFORCE_MISMATCH=1` 开一轮亦 pool 不变（Disabled 下永不触发，诚实对照））；
  XLEN/CH 涨；dashboard JSON 有效。
- [ ] **Step 3: 落库** plan 本表全✅＋EXEC_LOG append-only＋TASK_PLAN 步骤 14 ✅；
  禁未授权 commit——用户确认后统一提交。

---

## §3. 状态总览

| 子项 | 内容 | 优先级 | 状态 | 验收 |
|------|------|--------|------|------|
| P4-1 | mismatch 执法开关 | P0 | ✅ 已完成（2026-09-22：GeoVerdict＋apply_geo_verdict＋config/env；2 单测；143 过。诚实记录：verdict 复用冻结的 exit_matches_source 而非复制逻辑） | 纯判定＋失败映射 |
| P4-2 | GeoLite2 更新脚本＋文档 | P1 | ✅ 已完成（2026-09-22：`deploy/geoip_update.py` 干跑＋roundtrip＋OPERATION 定时任务＋compose；文件名连字符改下划线以便 import，沿仓惯例） | 干跑＋文档 |
| P4-3 | 门禁＋回归 | 门禁 | ✅ 已完成（2026-09-22：143 过/4 真 live/bench＋存量六用例＋E2E 重跑＋enforce 对照，见 EXEC_LOG 步骤 14 条） | 144±2＋E2E 重跑 |

## §4. 不做（explicitly out）

- **JA4 漂移门**（前置无变化，延续 OUT；风险溢价仍为诚实替代）。
- **库热加载**（memmap 常驻，重启生效；watchdog 另议）。
- **真 Key 灰度／Linux 50k**（仍待用户输入）。
- **dLinUCB／P2C**（延续 OUT）。

## Self-Review

1. **Spec coverage:** 执法→P4-1；库更新→P4-2；JA4 仍 out（§4＋F-核查）。
2. **Placeholder scan:** 无 TBD/TODO；沙箱不可触发点已声明两处（计划头＋P4-3）。
3. **Type consistency:** `GeoVerdict{Match,Mismatch,Skipped}`；`geo_verdict(Option,Option)->GeoVerdict`
   （looked_up 以 Option<&str> -fed，签名实现时锁定，单测先行）；
   `FreePoolConfig.geo_enforce: bool`；`note_free_verify("geo_fail")` 复用通用口。
4. **存量风险：** 默认关＝Phase 3 逐行等价（E2E 对照覆盖）；`exit_matches_source` 冻结；
   config 加字段——字面量构造点编译器指引同步。
5. **门禁诚实性：** release bandit 跳过（bandit 未动，省约 2min，注明理由）；
   enforce=1 对照轮预期 pool 不变（Disabled），如实记录为对照而非功能验证。
